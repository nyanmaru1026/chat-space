.side_bar
  .side-header
    .top-items
      .top-items-name 
        = current_user.name
      .top-items-icons
        = link_to "#" do
          = icon('fas', 'edit')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog')

  .side-bar-grouplist
    .group-name tech-camp
    .group-message まだ何もないよ